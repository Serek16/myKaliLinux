root="/home/kali/live-build-config"
alias main="cd $root"
kconfig="$root/kali-config"
variant="$kconfig/variant-default"
alias hooks="cd $variant/hooks/live"
alias packs="cd $variant/package-lists"

chroot="$variant/includes.chroot"
alias chroot="cd $chroot"
alias skel="$chroot/etc/skel"
alias udev="$chroot/etc/udev/rules.d"
alias profiled="$chroot/etc/profile.d"
alias kaliconf="$chroot/usr/lib/live/config"
alias lbin="$chroot/usr/local/bin"

alias chroot2="cd $root/config/includes.chroot"

alias build="cd $root && ./build.sh --verbose"
